/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com'], // tambahkan domain gambar dari luar yang kamu pakai
  },
};

module.exports = nextConfig;
